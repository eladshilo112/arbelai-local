# תרומה לפרויקט

תודה על הרצון לתרום.

## כללים

1. פתחו Issue או RFC לפני שינוי ארכיטקטוני.

2. כל שינוי התנהגותי חייב לכלול Tests.

3. אין להוסיף Telemetry, Proxy, Port, Service או שינוי גלובלי כברירת מחדל.

4. אין להוסיף תלות חדשה ללא ניתוח רישיון, אבטחה ועלות תחזוקה.

5. חתמו Commit בהתאם ל Developer Certificate of Origin באמצעות `git commit -s`.

6. אין לכלול Secrets, נתוני משתמש או מודלים ב Repository.

## פקודות

```powershell
cmd /c npm test
cmd /c npm run check
```